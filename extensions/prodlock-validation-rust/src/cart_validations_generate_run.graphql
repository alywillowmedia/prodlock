query Input {
  shop {
    metafield(namespace: "prodlock", key: "settings") {
      value
    }
  }
  cart {
    buyerIdentity {
      email
    }
    lines {
      merchandise {
        __typename
        ... on ProductVariant {
          id
          product {
            title
            metafield(namespace: "$app", key: "locked") {
              value
            }
          }
        }
      }
    }
  }
}
